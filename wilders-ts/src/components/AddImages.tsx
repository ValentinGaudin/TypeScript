function AddImages(props: {}) {
    return (
        <div>
            <form
                onSubmit={async (e) => {}}
            >

            </form>
        </div>
    );
}

export default AddImages;